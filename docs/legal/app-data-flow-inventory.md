# kukuri アプリ データフロー突合表

最終更新日: 2026-09-19
対象: kukuri デスクトップアプリ legal bundle version 8

この表は、`docs/legal/privacy-policy.md` と `docs/legal/external-transmission-notice.md` を現行実装へ照合するための記録である。各 Community Node が独自に処理する内容は、その Node の manifest と公開文書を正とする。

| データ種別 | Canonical source／端末内保持 | 複製・送信先と契機 | 目的 | 保持主体・削除限界 | 実装・仕様根拠 |
|---|---|---|---|---|---|
| アカウントを識別する秘密鍵 | OS keyring または端末内 identity file。通常の network payload に含めない | 利用者が鍵 export を明示実行した場合だけ暗号化ファイルへ出力する | 署名、同じアカウントの復元 | 端末または利用者が保存した export。P2P copy の削除とは無関係 | `crates/desktop-runtime/src/identity.rs`、`docs/legal/account-key-export-data-classification.md` |
| 公開鍵・endpoint ID・接続情報 | 公開鍵は署名済み object と account state、docs author の id（アカウントの署名鍵から導出した docs の書き込み用の公開鍵。ADR 0053）は投稿の envelope と docs の entry と gossip の hint、endpoint ID／address hint は runtime・discovery state | 公開鍵は対象 replica の参加者、endpoint 情報は接続 peer、DHT、選択した Community Node／relay。IP address は実通信の相手方・経路事業者から観測可能 | 署名検証、peer discovery、P2P 接続確立、relay | 相手方・DHT・Node・relay の保持は各主体の方針に従う。端末削除で第三者の接続記録は消えない | `crates/transport/src/{config.rs,discovery.rs,iroh/**}`、ADR 0008／0009 |
| プロフィール・プロフィール画像・公開フォロー関係 | author-owned public docs replica。プロフィール画像の canonical source は `picture_asset` が参照する content-addressed blob、SQLite は再構築可能な投影。外部画像 URL field は profile／view／IPC contract に持たず、client は任意の画像 host を自動取得しない | profile metadata と画像 blob は同じ author replica／topic を取得する P2P peer。対応する Community Node が索引に参加する場合は当該 Node。取得後の UI は端末内 object／asset URL を使う | 公開プロフィール・social graph の表示、同期、検索・発見、プロフィール画像の取得 | 受信 peer と参加 Node が metadata／blob の copy／index を保持し得る。削除・訂正は対応実装が認識する範囲に限られる | ADR 0013／0015／0019、`crates/core/src/profile.rs`、`crates/app-api/src/{social.rs,service/profile_docs_support.rs}`、`apps/desktop/src/shell/presentation.ts`、`apps/desktop/src-tauri/tauri.conf.json` |
| 公開投稿・返信・再投稿・リアクション | 対象 topic／author docs replica、添付は blob。SQLite は投影 | 対象 topic の P2P peer。Node が supported topic の docs sync／index に参加する場合は当該 Node。hint は更新通知 | 表示、同期、返信、検索・発見、添付取得 | 受信済み peer、Node index／cache に copy が残り得る。撤回は network 全体からの即時完全削除ではない | ADR 0003／0004／0016／0017／0025／0032、`crates/docs-sync/**`、`crates/blob-service/**` |
| private channel | channel capability と暗号化対象 replica。local projection を持つ | capability を持つ参加 peer。利用者が private indexing を明示許可した場合だけ対象 Node | 限定 audience 内の同期・表示、明示時の索引 | 参加 peer と明示許可した Node。参加者が既取得の copy を遠隔完全消去できない | ADR 0018／0025、`crates/app-api/src/private_channels.rs`、`crates/desktop-runtime/src/community_node/indexing_request_support.rs` |
| DM | pairwise DM docs／blob と local projection | 指定した相手の peer。保管機能を明示利用する Node がある場合は当該 Node の個別文書に従う | 1対1メッセージ同期・添付取得 | 送受信端末。相手が取得済みの copy は送信側だけでは消去できない | ADR 0020、`crates/app-api/src/direct_messages.rs` |
| DHT・P2P・relay 通信 | runtime の discovery／connection state | Mainline DHT、接続 peer、構成された iroh relay。経路は `Direct P2P -> Relay Supported P2P -> Relay Fallback` | 到達先探索、hole punching、P2P transport、成立しない場合の実データ中継 | DHT／peer／relay 側の保持方針に従う。relay URL があるだけでは実データ fallback を意味しない | `crates/transport/src/iroh/**`、`docs/architecture/p2p-first-community-node-responsibility-boundary.md` |
| Community Node 認証・consent・rendezvous | Node token／Node 別同意は端末 secure storage、subscriber／consent／presence は Node 側 | 利用者が保存し同意した Node。認証時の公開鍵・proof、session metadata、topic rendezvous key、接続用 endpoint 情報 | 認証、consent、bootstrap assist、topic rendezvous、Node capability | local token／同意と Node 側 record。topic presence は TTL。詳細は各 Node の manifest／privacy／retention | ADR 0009、`crates/desktop-runtime/src/community_node/**`、`docs/legal/community-node-legal-documents-data-classification.md` |
| Community Node 成人向け表現の推定の照会（#1056） | 照会結果（post id / blob hash → 推定）は表示中の一時状態と Rust 側取得ゲートの in-memory 集合だけに保持し、永続化しない。Node ごとの採用設定は community node 設定ファイル（`content_advisory_enabled`） | 推定の採用を選び、認証と必須同意が成立し、manifest で発行元を確認できた設定済み Node だけ。タイムライン・スレッド・ブックマーク・プロフィール・通知に表示した投稿の ID と添付の blob hash（`POST /v1/advisories/lookup`）。本文、閲覧履歴の集約、social graph は送らない | 採用 Node が推定した成人向け表現の代替表示と、表示設定 OFF 中のメディア取得停止 | 当該 Node の retention に従う。端末側は再起動で消える。採用を OFF にした Node へは以後照会しない | ADR 0046 §6、`crates/desktop-runtime/src/community_node/content_advisory_lookup_support.rs`、`apps/desktop/src/shell/data/useTimelineContentAdvisoryLookup.ts`、`crates/cn-user-api/src/handlers/advisories.rs` |
| Community Node へのブロック・ミュートの提供（#1061） | 送信待ちと提供の選択は account DB の隣のファイル（`<db>.trust-observations.json`）。mute の canonical は端末内 `muted_authors`、block は署名済み author replica のまま変えない | その Node の任意同意文書（`trust_observation_sharing`）へ同意した Node だけ。相手の公開鍵、操作の種別（block / mute）と状態、操作時刻、自分の署名（`POST /v1/trust/observations`）。投稿本文・閲覧履歴・mute 一覧の全量は送らない（既存分は同意時に選んだ場合だけ） | 閲覧者ごとの relation 値の調整（ADR 0026 §8） | Node 側は有効 180 日 / 解除 30 日で削除。提供の停止・同意取消・Node の削除で、保存済み観測の削除を要求する | ADR 0026 §8、ADR 0022 追補、`crates/desktop-runtime/src/community_node/trust_observation_support.rs`、`crates/cn-user-api/src/handlers/trust_observations.rs` |
| Community Node への信頼評価の照会（#1061） | 照会結果（著者 pubkey → 折りたたみ判断）は表示中の一時状態だけに持ち、永続化しない。採用順位は community node 設定ファイル（`trust_node_priority`）、作者ごとの表示例外は `<db>.trust-display.json` | 採用順位に選び、認証と必須同意が成立した Node だけ。タイムライン・スレッド・プロフィール・ブックマークに表示した投稿の著者と引用元著者、および表示した live / game 一覧の主催者の公開鍵（`POST /v1/trust/evaluations`）。本文・閲覧履歴・ブロック / ミュート一覧は送らない | 評価の低い作者の投稿の折りたたみ（ADR 0026 §8.4） | 当該 Node の retention に従う。端末側は評価の期限（既定 600 秒）で照会し直し、作り直せなければ期限から最大 60 秒で捨てる。再起動でも残さない。採用順位が空なら照会しない | ADR 0026 §8、`crates/desktop-runtime/src/community_node/trust_gate_support.rs`、`apps/desktop/src/shell/data/useAuthorTrustGateLookup.ts`、`crates/cn-user-api/src/handlers/trust_relation.rs` |
| Community Node 検索・索引・通報・tester feedback | Node-local index／case／feedback record。client は結果・local observation を保持し得る。索引状況（自分の申請状態・対象の索引対象判定）は表示中だけ保持し、永続化しない | 利用者が選択した Node。検索 query、対象識別子、indexing request、索引状況の確認（対象識別子。非公開チャンネルの索引対象判定は明示確認後の所属証明を伴う）、通報理由・任意詳細・任意連絡先、feedback 内容 | 検索・発見、明示的な索引参加と自分の申請状態・索引対象の確認、通報、安全性対応、Preview 改善 | 当該 Node の retention と法的保全に従う。他 Node や network 全体へ自動適用されない | ADR 0025／0030／0033／0039、`crates/desktop-runtime/src/community_node/{index_query_support.rs,indexing_request_support.rs,indexing_status_support.rs,report_routing_support.rs,tester_feedback_support.rs}` |
| アプリ同意・18歳以上の自己申告 | `<db_path>.app-consent.json`。slug／version／時刻／表示言語／app version／build の種別と年齢自己申告の事実だけ | network へ送信しない。端末 backup／restore でも再同意・再申告を求める | network 開始前 gate、重要変更時の再同意 | 端末内のみ。生年月日・身分証を収集しない | `apps/desktop/src-tauri/src/{state.rs,commands/app_consent.rs}`、`docs/legal/{app-consent,age-attestation}-data-classification.md` |
| 公開投稿の外部 URL preview（#1174） | URL は既存公開投稿本文。sanitized OGP metadata／raster image は process-memory cache（success 10分／failure 60秒、128件かつ16 MiB） | 同意後、公開・表示可能・settledな投稿cardがviewport内に入ったとき、primary content先頭URLのlink先と任意OGP画像hostへGET。IP address、HTTP／TLS metadata、path／query、固定User-Agentが観測され得る。cookie、Authorization、Referer、account／topic／post情報、他本文は送らない | site／title／説明／任意画像のpreview表示 | cacheはprocess終了で消える。link先、image host、経路事業者の保持は各主体の方針に従う。private channel／DM／折りたたみ中は自動取得しない | ADR 0051、`docs/legal/link-preview-data-classification.md`、`apps/desktop/src-tauri/src/commands/link_preview.rs`、`apps/desktop/src/components/core/LinkPreviewCard.tsx` |
| 自動更新確認 | Direct／NSIS・Linux版のupdate stateはmemory。manifest URLはTauri配布設定。Microsoft Store版はStore／Windows管理 | Direct／NSIS・Linux版だけが、同意後のshell起動時と30分ごと、および手動確認時にGitHub Releasesへrequest。IP address、HTTP／TLS上必要なrequest metadata、現在版／platformに応じたupdater requestが相手方から観測可能。Microsoft Store版はkukuri内からGitHub Releasesにも`StoreContext`等にも更新確認を送らない | Direct配布の署名済みPreview buildの確認・取得。Store版の更新はMicrosoft Store／Windowsへ委譲 | GitHubと経路事業者の方針に従う。kukuriはDirect版の取得結果とerrorをruntime中だけ保持。Store側の処理はMicrosoft／Windowsの方針に従う | `apps/desktop/src-tauri/{tauri.conf.json,tauri.microsoft-store.conf.json,src/app_update.rs}`、`apps/desktop/src/{lib/distribution.ts,shell/DesktopShellPage.tsx,components/settings/ReleasePanel.tsx}` |
| 診断レポート | UI が現在の state から生成。アプリ版、platform／user agent、接続・delivery／discovery path、peer／topic 件数、未読件数、更新・通知状態、Node URL／session／retry／error | 生成・preview・clipboard／file export は local。利用者が GitHub 等へ添付した場合だけその送信先 | Preview 不具合の調査 | export 後は利用者が選んだ送信先。秘密鍵、auth token、private capability、invite／share token、DM 本文、local DB path は既定 report に含めない | `apps/desktop/src/lib/releaseReadiness.ts`、`apps/desktop/src/components/settings/ReleasePanel.tsx` |
| アプリ内ログ(開発者モード) | Tauri backend の `tracing` event を process 内の in-memory ring buffer(2,000 件 / 1 MiB / 1 行 4 KiB。超過は古い行から破棄)に保持。file・DB・設定へは保存しない | 開発者モードON時に設定 > 開発者 で表示し、clipboard／`kukuri-logs.txt` への書き出しは利用者操作だけ。利用者が GitHub 等へ添付した場合だけその送信先 | Preview 不具合の調査(Windows のようにコンソールが無い環境を含む) | process 終了で消える。書き出し後は利用者が選んだ送信先。秘密鍵、auth token、DM 本文、パスフレーズはログへ出力しない契約。peer / topic / Node URL / local path 等の識別子は含まれ得るため共有前に除く | `apps/desktop/src-tauri/src/tracing.rs`、`apps/desktop/src-tauri/src/commands/developer_logs.rs`、`apps/desktop/src/components/settings/DeveloperLogViewer.tsx`、`docs/legal/developer-log-viewer-data-classification.md` |
| 行動分析・自動 crash report | 現行アプリは収集基盤を持たない | 送信しない | 該当なし | 導入前に本文書改訂と必要な同意を行う | `apps/desktop/src-tauri/tauri.conf.json`、Community Node capability は各 Node manifest で別途開示 |

## 確認上の境界

- `api.kukuri.app` は現行配布物の初期候補であり、汎用 runtime の固定宛先ではない。利用者は候補を削除・置換できる。
- `iroh-relay.kukuri.app` を含む relay URL は、選択した Node の connectivity metadata や runtime config から得る。任意 Node が別の relay を提示し得る。
- Community Node 運営者と kukuri 運営者が同一とは限らない。Node ごとの受信項目、保持、外部送信、削除・通報は当該 Node の公開文書を確認する。
- P2P で受信済みの第三者 copy、第三者 Node の index／cache、経路事業者の log を kukuri アプリから一括消去することはできない。
